; PlatformIO Project Configuration File
; https://docs.platformio.org/page/projectconf.html
;
; Environments:
;   flight  — production flight firmware   (pio run -e flight -t upload)
;   bench   — hardware bench test harness  (pio run -e bench  -t upload)

[platformio]
default_envs = bench

[common]
platform      = teensy
board         = teensy40
framework     = arduino
monitor_speed = 115200
; monitor_port  = COM3        ; Windows — change to your port
; monitor_port = /dev/ttyACM0       ; Linux
; monitor_port = /dev/cu.usbmodem*  ; Mac
build_flags =
    -Wall
    -Wextra
    -Wno-unused-parameter
    -I include

; ── Production flight firmware ─────────────────────────────────────────────
[env:flight]
extends = common
build_src_filter = +<*> -<main.cpp> -<bench_test.cpp> -<test_alt_estimator.cpp>

; ── Hardware bench test harness ────────────────────────────────────────────
[env:bench]
extends = common
build_src_filter = +<*> -<main.cpp> -<main_control_loop.cpp> -<test_alt_estimator.cpp>

; ── Custom Main Entry Point ─────────────────────────────────────────────
[env:run_main]
extends = common
build_src_filter = +<*> -<main_control_loop.cpp> -<bench_test.cpp> -<test_alt_estimator.cpp>
